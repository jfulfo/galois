def compose(f, g) {
    def composed(x) {
        f(g(x));
    }
    composed;
}

def id(x) {
    x;
}

def wrap(x) {
    def inner(y) {
        y;
    }
    inner(x);
}

def main() {
    func = compose(wrap, id);
    func("hello world");
}

main();
